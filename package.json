{
  "name": "summon-author-risuai-plugin",
  "version": "1.1.3",
  "private": true,
  "description": "RisuAI API 3.0 plugin for a separate AI writers' room and memo workflow",
  "scripts": {
    "build": "node scripts/build.cjs",
    "check": "tsc --project tsconfig.json --noEmit",
    "test:built": "node scripts/test-built.cjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "markdown-it": "^15.0.0"
  }
}
